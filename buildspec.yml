version: 0.1
phases:
  pre_build:
    commands:
      - echo "pre build"
  build:
    commands:
      - echo "build"
  post_build:
    commands:
      - echo "post buld"
      - touch "built.txt"
artifacts:
  files: built.txt
  discard-paths: yes
